server:
  port: 18082

spring:
  application:
    name: store-service

  datasource:
    url: jdbc:postgresql://localhost:5432/poppopjubjub
    username: localhost
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18080/eureka/
